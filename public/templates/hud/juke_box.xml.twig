<?xml version="1.0" encoding="UTF-8"?>
<manialink id="{{ id }}">
    <frame>
        {# === BackGround (mabye needs be bigger) === #}
        <quad 
            posn="-61.9 47 -32" 
            sizen="7.8 9.6" 
            style="Bgs1" 
            substyle="BgList">
        </quad>
        {# === ICONS === #}
        <quad 
            posn="-58.2 43.3 -31" 
            sizen="5.8 5.9"
            halign="center" 
            valign="center"
            action="{{ data.list_id }}"
            image="{{ asset('mapList') }}"
            imagefocus="{{ asset('mapListHover') }}">
        </quad>
        <label 
            posn="-57.9 39 -31" 
            sizen="6.6 1.3" 
            halign="center" 
            valign="center"
            textsize="0.9" 
            textcolor="FFF"
            text="MAPS {{ data.total_maps }}">
        </label>
    </frame>
</manialink>